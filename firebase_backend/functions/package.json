{
  "name": "crii-firebase-functions",
  "private": true,
  "main": "lib/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "serve": "firebase emulators:start --only functions,firestore",
    "deploy": "firebase deploy --project crii-focus-today --only functions,firestore:rules,firestore:indexes,storage"
  },
  "dependencies": {
    "debug": "^4.4.3",
    "firebase-admin": "^13.4.0",
    "firebase-functions": "^6.6.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
