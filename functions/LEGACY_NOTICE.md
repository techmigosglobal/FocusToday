## Legacy Functions Directory

This `functions/` folder is kept for historical reference only.

Active deployable Cloud Functions source for this project is:

- `firebase_backend/functions`

`firebase.json` points to `firebase_backend/functions` as the live source of truth.
